// Default page margin values in pixels, matching a standard letter-size document layout.
// Used by both the Ruler (visual) and the Editor (padding) to stay in sync.
export const LEFT_MARGIN_DEFAULT = 56;
export const RIGHT_MARGIN_DEFAULT = 56;